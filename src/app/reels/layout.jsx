/** @type {import('next').Metadata} */
export const metadata = {
  title: "Reels · Giles Lamb",
  description:
    "Selected audio and audiovisual reels by category. Installation, museum, animation, documentary, TV, trailers.",
};

export default function ReelsLayout({ children }) {
  return children;
}
