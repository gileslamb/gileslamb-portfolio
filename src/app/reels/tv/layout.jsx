/** @type {import('next').Metadata} */
export const metadata = {
  title: "Giles Lamb · TV",
  description:
    "Music for television across drama, factual and entertainment. Themes, underscore and title sequences for UK and international broadcast.",
};

export default function TVLayout({ children }) {
  return children;
}
