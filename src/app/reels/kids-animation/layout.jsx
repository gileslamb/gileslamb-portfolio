/** @type {import('next').Metadata} */
export const metadata = {
  title: "Giles Lamb · Kids & Animation",
  description:
    "Original scores and themes for children's animation and kids' TV across BBC, Sky, CBBC and independent productions. Playful, characterful, emotionally precise.",
};

export default function KidsAnimationLayout({ children }) {
  return children;
}
